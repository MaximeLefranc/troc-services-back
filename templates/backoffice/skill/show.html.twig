{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skill.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ skill.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backoffice_skills') }}">Retour à la liste</a>

    <a href="{{ path('edit_skill', {'id': skill.id}) }}" clacc="btn">Editer</a>

    {{ include('backoffice/skill/_delete_form.html.twig') }}
{% endblock %}
