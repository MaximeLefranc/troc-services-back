{% extends 'base.html.twig' %}

{% block title %}Editer le skill {{skill.name}}{% endblock %}

{% block body %}
    <h1>Editer le skill {{skill.name}}</h1>

    {{ include('backoffice/skill/_form.html.twig', {'button_label': 'Editer'}) }}

    <a href="{{ path('backoffice_skills') }}" class="btn" >Retour à la liste</a>

    {{ include('backoffice/skill/_delete_form.html.twig') }}
{% endblock %}
