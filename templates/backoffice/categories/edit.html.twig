{% extends 'base.html.twig' %}

{% block title %}éditer la catégorie {{category.name }}{% endblock %}

{% block body %}
    <h1>Editer la catégorie {{category.name }}</h1>
    <form ></form>
    {{ include('backoffice/categories/_form.html.twig', {'button_label': 'Editer'}) }}

    

    <a href="{{ path('backoffice_categories_index') }}" class="btn btn-primary" >Retour à la liste</a>

    {{ include('backoffice/categories/_delete_form.html.twig') }}
{% endblock %}
